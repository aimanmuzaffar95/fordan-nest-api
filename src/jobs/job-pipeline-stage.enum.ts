export enum JobPipelineStage {
  LEAD = 'lead',
  QUOTED = 'quoted',
  WON = 'won',
  PRE_METER_SUBMITTED = 'pre_meter_submitted',
  PRE_METER_APPROVED = 'pre_meter_approved',
  SCHEDULED = 'scheduled',
  INSTALLED = 'installed',
  POST_METER_SUBMITTED = 'post_meter_submitted',
  COMPLETED = 'completed',
  INVOICED = 'invoiced',
  PAID = 'paid',
}
