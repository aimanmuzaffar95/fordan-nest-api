services:
  api:
    build:
      target: builder
    environment:
      NODE_ENV: development
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    command: npm run start:dev
    volumes:
      - .:/app
      - /app/node_modules
